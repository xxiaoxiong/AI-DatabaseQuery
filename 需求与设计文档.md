# AI-DatabaseQuery：基于 DeepSeek 的自然语言数据库查询可视化平台
## 需求与设计文档 v1.1

> **关键决策记录**
> - **部署方式**：内网私有化部署，数据不出内网
> - **大模型接入**：前期使用 DeepSeek API Key；后期切换为内网自部署大模型（接口兼容 OpenAI 协议）
> - **MVP 范围**：单用户本地版，无需注册/登录体系，快速验证核心链路

---

## 1. 项目背景与目标

### 背景

企业数据查询能力长期被"会写 SQL 的人"垄断，业务人员依赖数据团队才能获取数据，效率低、响应慢。DeepSeek 在中文理解和代码生成上的优秀表现，使"用自然语言查数据库"成为真正可落地的方案。

### 目标

| 目标 | 描述 |
|------|------|
| **降低门槛** | 让零 SQL 基础的用户能独立完成数据查询 |
| **提升效率** | 从"提需求→等数据"变为"问问题→看结果" |
| **AI 增强分析** | 不仅返回数据，还提供 AI 解读、趋势分析、异常检测 |

> 核心价值：**"用说话的方式查数据，用图表的方式看结果，用 AI 的方式理解数据。"**

---

## 2. 用户画像与典型场景

### 目标用户

| 角色 | 核心诉求 |
|------|----------|
| **业务人员** | 快速自助查询，不依赖技术团队 |
| **管理层** | 一句话得到报表，AI 给出解读 |
| **数据分析师** | 用自然语言加速查询，专注分析 |
| **产品/运营** | 灵活查询用户行为数据，快速出图 |

### 典型场景

- **场景 A**：输入"上个月华东区各城市销售额从高到低" → 生成 SQL → 柱状图 + AI 解读
- **场景 B**：输入"对比今年和去年同期用户注册量趋势" → 折线对比图 + AI 差异分析
- **场景 C**：输入"最近7天有没有哪天订单量异常" → 异常点高亮 + 可能原因
- **场景 D**：输入"帮我分析用户留存情况" → AI 拆解问题 → 多子查询 → 综合报告

---

## 3. 功能需求

### 3.1 数据源管理

| 功能 | 优先级 |
|------|--------|
| 添加/编辑/删除数据库连接（host/port/user/password/db）| P0 |
| 一键连接测试 | P0 |
| 多数据源切换 | P0 |
| Schema 自动探索（表结构、字段名、类型、注释）| P0 |
| Schema 手动标注业务含义（增强 AI 理解）| P1 |

**支持数据库**：MySQL、PostgreSQL（P0）；SQLite、SQL Server（P1）；ClickHouse、Oracle（P2）

### 3.2 自然语言查询（核心）

| 功能 | 优先级 |
|------|--------|
| 中英文自然语言输入 | P0 |
| NL→SQL 转换（DeepSeek API）| P0 |
| SQL 预览与手动编辑 | P0 |
| 一键执行查询 | P0 |
| 上下文多轮对话（追问）| P1 |
| 查询意图澄清（问题模糊时 AI 追问）| P1 |
| SQL 执行报错自动修复 | P1 |
| 查询建议推荐 | P1 |

### 3.3 SQL 编辑器

| 功能 | 优先级 |
|------|--------|
| 语法高亮 | P0 |
| 表名/字段名自动补全 | P1 |
| 一键格式化 | P1 |
| AI 解释选中 SQL 片段 | P1 |
| AI 性能优化建议 | P2 |

### 3.4 数据可视化

| 功能 | 优先级 |
|------|--------|
| 数据表格（分页、排序、搜索）| P0 |
| AI 自动推荐图表类型 | P0 |
| 柱状图、折线图、饼图 | P0 |
| 散点图、热力图 | P1 |
| 图表配置（颜色、标题、轴标签）| P1 |
| 图表导出（PNG/SVG/CSV）| P1 |
| 地图可视化（省市级）| P2 |

### 3.5 AI 分析增强

| 功能 | 优先级 |
|------|--------|
| 数据摘要解读（每次查询自动触发）| P0 |
| 趋势分析（时间序列自动识别）| P1 |
| 异常检测（标记异常点 + 可能原因）| P1 |
| 多结果对比分析 | P1 |
| 分析报告生成（Markdown/PDF）| P2 |

### 3.6 其他模块

| 功能 | 优先级 |
|------|--------|
| 查询历史记录（自然语言 + SQL + 结果摘要）| P0 |
| 收藏常用查询 | P1 |
| 用户注册/登录（JWT）| **P2（单用户版跳过）** |
| 数据源/表级别权限控制 | P2（多用户版再做）|
| 操作审计日志 | P2 |
| 仪表盘（多查询组合，拖拽布局）| P2 |

---

## 4. 非功能需求

| 类别 | 指标 |
|------|------|
| NL->SQL 响应时间 | < 5秒（P95）|
| 普通查询执行时间 | < 3秒（< 10万行）|
| 系统可用性 | ≥ 99.5%（内网环境）|
| SQL 安全 | 白名单校验，只允许 SELECT |
| 密码存储 | AES-256 加密 |
| 数据传输 | 内网 HTTP（可选 HTTPS）|
| 数据隐私 | 所有数据不出内网，仅 Schema+问题发送给大模型 |

---

## 5. 系统架构

```
【内网环境】
┌──────────────────────────────────────────────────────┐
│                   前端（Browser）                      │
│  React + TypeScript + TailwindCSS + ECharts           │
│  [查询对话框] [SQL编辑器] [可视化面板] [AI分析面板]      │
└──────────────────────┬───────────────────────────────┘
                       │ REST API / WebSocket（内网）
┌──────────────────────▼───────────────────────────────┐
│                  后端（Python FastAPI）                 │
│  ┌────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ NL2SQL引擎  │  │  查询执行引擎  │  │  AI分析引擎   │  │
│  └─────┬──────┘  └──────┬───────┘  └──────┬───────┘  │
│        │                │数据库连接池        │          │
└────────┼────────────────┼──────────────────┼──────────┘
         │ LLM接口层       │                  │ LLM接口层
    ┌────▼──────────────────────────────────▼────┐
    │           LLM 接入层（统一 OpenAI 协议）       │
    │  前期：DeepSeek API Key（外网）               │
    │  后期：内网自部署大模型（Ollama / vLLM 等）    │
    │  切换只需修改 base_url + api_key 配置         │
    └────────────────────────────────────────────┘
         │
    ┌────▼────┐    ┌───────────────┐
    │系统数据库 │    │ 用户业务数据库  │
    │(SQLite) │    │ (MySQL/PG/...)│
    └─────────┘    └───────────────┘
```

### 核心数据流

```
自然语言输入
  → [上下文构建器] 拼接 Schema + 历史对话 + 问题
  → [DeepSeek API] 生成 SQL
  → [SQL 安全校验] 只允许 SELECT
  → [查询执行引擎] 执行并返回结果
  → [AI 分析引擎] 生成数据解读
  → [可视化推荐] 自动选择图表类型
  → 前端展示
```

---

## 6. 核心模块设计

### 6.1 NL2SQL Prompt 设计

```
System: 你是专业数据库查询助手，只生成 SELECT 语句，返回 JSON 格式。
        数据库方言：{dialect}
        Schema：{schema_context}
        历史对话：{conversation_history}

输出格式：
{
  "sql": "SELECT ...",
  "explanation": "查询含义说明",
  "clarification": null,       // 问题模糊时填写追问
  "chart_suggestion": "bar",   // 推荐图表类型
  "confidence": 0.95
}
```

**Schema 压缩策略**（表过多时）：关键词匹配 → 向量相似度检索 → 历史高频表优先

### 6.2 可视化推荐规则

| 数据特征 | 推荐图表 |
|----------|----------|
| 1维度 + 1数值，行数 ≤ 10 | 饼图 |
| 1维度 + 1数值，行数 > 10 | 柱状图 |
| 1时间列 + 1数值列 | 折线图 |
| 1时间列 + 多数值列 | 多系列折线图 |
| 2数值列 | 散点图 |
| 2维度 + 1数值 | 热力图 |
| 其他 | 数据表格 |

### 6.3 安全防护（三层）

1. **语句白名单**：只允许 `SELECT`，拦截 `INSERT/UPDATE/DELETE/DROP/CREATE` 等
2. **函数黑名单**：拦截 `SLEEP()`, `BENCHMARK()`, `LOAD_FILE()` 等
3. **只读账号**：推荐用户配置只读数据库账号，从根本上防止数据修改

---

## 7. 技术选型

### 前端

| 技术 | 选型 |
|------|------|
| 框架 | React 18 + TypeScript |
| 样式 | TailwindCSS + shadcn/ui |
| 图表 | Apache ECharts |
| SQL 编辑器 | CodeMirror 6 |
| 状态管理 | Zustand |
| 数据请求 | Axios + React Query |
| 构建 | Vite |

### 后端

| 技术 | 选型 |
|------|------|
| 框架 | FastAPI（Python）|
| ORM | SQLAlchemy 2.0（异步）|
| 系统数据库 | mysql:3306|
| 缓存 | 内存缓存（单用户版暂不引入 Redis）|
| 认证 | **单用户版跳过**，后期多用户版引入 JWT |
| 加密 | cryptography（Fernet/AES-256，用于数据库密码加密）|

### AI 集成

| 技术 | 选型 |
|------|------|
| LLM SDK | openai Python SDK（统一接口，兼容 DeepSeek / Ollama / vLLM）|
| 前期模型 | DeepSeek API（deepseek-chat）|
| 后期模型 | 内网自部署模型（如 Qwen2.5-Coder、DeepSeek-R1 本地版）|
| 切换方式 | 修改配置文件中 `LLM_BASE_URL` 和 `LLM_API_KEY` 即可，代码零改动 |
| Embedding| modelscope下载到本地 bge-small-1.5 模型 |

---

## 8. 页面结构与交互

### 页面路由

```
【单用户版（MVP）- 无登录体系】
/                -> 直接进入查询工作台（跳过登录）
/query           -> 查询工作台（核心页面）
  左侧：Schema 浏览器
  中上：自然语言输入 / SQL 编辑器（Tab 切换）
  中下：查询结果（图表 / 表格 Tab）
  右侧：AI 分析面板（可收起）
/history         -> 查询历史
/datasources     -> 数据源管理
/settings        -> 设置（LLM Base URL / API Key 配置）

【后期多用户版 - 补充】
/login           -> 登录页
/admin           -> 用户与权限管理
```

### 查询工作台交互流程

```
1. 输入自然语言问题 → Ctrl+Enter 提交
2. 显示"AI 正在理解您的问题..."
3. 展示生成的 SQL（可折叠，支持手动编辑）
4. 自动执行 SQL，显示进度
5. 结果展示：AI 推荐图表 + 数据表格 Tab + 右侧 AI 解读
6. 底部支持追问（多轮对话）
```

### UI 设计原则

- **渐进式披露**：默认隐藏 SQL，用户可展开查看/编辑
- **即时反馈**：每步骤有明确加载状态
- **错误友好**：执行失败时 AI 自动给出修复建议
- **响应式**：支持桌面和平板

---

## 9. 开发路线图

### Phase 1 - 单用户本地版 MVP（3-4 周）：核心链路跑通

> 目标：无需登录，直接配置数据源和 LLM Key，即可完整体验核心功能

- [ ] 前后端脚手架搭建（Vite + FastAPI）
- [ ] LLM 接入层封装（统一 OpenAI 协议，base_url 可配置）
- [ ] 数据源管理（MySQL + PostgreSQL，密码加密存储）
- [ ] Schema 自动读取
- [ ] NL→SQL 转换（DeepSeek API Key 接入）
- [ ] SQL 安全校验（SELECT 白名单）
- [ ] 查询执行 + 结果表格展示
- [ ] 基础图表（柱状图、折线图、饼图）
- [ ] AI 数据摘要解读
- [ ] 查询历史记录（SQLite 本地存储）
- [ ] 设置页面（LLM Base URL / API Key 配置）

### Phase 2 - 增强（4 周）：体验与 AI 能力

- [ ] SQL 编辑器（CodeMirror，高亮 + 补全）
- [ ] 多轮对话上下文
- [ ] 查询意图澄清 & 错误自动修复
- [ ] 趋势分析 + 异常检测
- [ ] 多结果对比分析
- [ ] Schema 手动标注
- [ ] 图表配置与导出

### Phase 3 - 内网多用户版（4 周）：企业级功能

- [ ] 切换为内网自部署大模型（修改 base_url 配置）
- [ ] 用户注册/登录（JWT）
- [ ] 权限管理（数据源级别、表级别）
- [ ] 仪表盘（拖拽布局，定时刷新）
- [ ] 分析报告生成（PDF 导出）
- [ ] 操作审计日志
- [ ] 更多数据库支持（ClickHouse、MongoDB）

---

## 10. 风险与约束

| 风险 | 缓解措施 |
|------|----------|
| **NL2SQL 准确率不足** | SQL 预览可编辑；用户反馈标注；持续优化 Prompt |
| **Schema 过大超出上下文** | Schema 压缩策略；向量检索相关表 |
| **DeepSeek API 不稳定（前期）** | LLM 接入层统一封装，后期无缝切换内网模型；缓存常用查询 |
| **数据库安全风险** | 只读账号 + SQL 白名单 + 连接信息加密 + 审计日志 |
| **大数据量查询超时** | 超时限制（30s）+ 行数限制（10,000行）+ 异步查询 |
| **用户数据隐私** | 只发送 Schema 结构和问题给 DeepSeek，不发送实际数据 |

---

## 附录：主要 API 接口

```
# 数据源
POST   /api/datasources              创建数据源
GET    /api/datasources              获取列表
POST   /api/datasources/{id}/test    测试连接
GET    /api/datasources/{id}/schema  获取 Schema

# 查询
POST   /api/query/nl2sql             自然语言转 SQL
POST   /api/query/execute            执行 SQL
GET    /api/query/history            查询历史

# AI 分析
POST   /api/ai/analyze               数据分析解读
POST   /api/ai/compare               多结果对比

# 设置（单用户版）
GET    /api/settings                 获取当前配置
PUT    /api/settings                 更新 LLM Base URL / API Key 等

# 用户（多用户版，Phase 3 实现）
POST   /api/auth/register            注册
POST   /api/auth/login               登录
GET    /api/auth/me                  当前用户信息
```
